{% extends 'layout.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="row" style="margin-top:100px;">
        <div class="col-md-12" align="center" style="color:#ffffff;">
            <h1>Analyse ton site</h1>
        </div>
    </div>
    <br>
    <div class="row">
        <div class="col-md-2">

        </div>
        <div class="col-md-8" >

            <div class="row" >
                <div class="col-md-10">
                    {% form_theme form 'bootstrap_4_layout.html.twig' %}
                    {{ form_start(form, { 'attr': {'novalidate': 'novalidate',"class":"" } }) }}
                    {{ form_widget(form) }}
                </div>
                <div class="col-md-2">
                    <button  class="btn btn-dark btn-lg"><i class="fa fa-search"></i> </button>
                    {{ form_end(form) }}
                </div>
            </div>

        </div>
        <div class="col-md-2">

        </div>
    </div>

{% endblock %}
